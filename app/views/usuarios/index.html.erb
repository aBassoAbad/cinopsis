<%= render "shared/titulo_pagina", titulo: "Usuarios" %>
<div class="center list-group">
  <% @usuarios.each do |usuario| %>
    <ul class="list-group-item listing">
      <div class="row">
        <%= link_to "", usuario_path(usuario) %></li>
        <%= link_to usuario.nombre.capitalize, 
                                        usuario_path(usuario) %></li>
        <% if logged_in? && (usuario_actual != usuario) %>
          <div>
          <% if !Friendship.find_by(usuario_id: usuario_actual.id, amigo_id: usuario.id) %>
            <%= button_to "Añadir a amigos", anadir_amigos_usuario_path(usuario), method: :post, class: "btn btn-xs btn-primary" %>
          <% else %>
            <%= button_to "Borrar a amigos", borrar_amigos_usuario_path(usuario), method: :delete, class: "btn btn-xs btn-danger" %>
          <% end %>
          </div>
        <% end %>
      </div>
    </ul>      
  <% end %>
</div>